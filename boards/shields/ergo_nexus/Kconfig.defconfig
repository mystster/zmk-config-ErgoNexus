if SHIELD_ERGO_NEXUS_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "ErgoNexus"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ERGO_NEXUS_LEFT || SHIELD_ERGO_NEXUS_RIGHT

config ZMK_SPLIT
    default y

endif
